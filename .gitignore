# Build output: the deploy zip and its extracted copy are generated by
# .claude/tools/make-deploy-zip.ps1 from the files tracked here.
/deploy/

# OS and editor noise
Thumbs.db
desktop.ini
.DS_Store
